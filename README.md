# Use-static-variable-to-count-number-of-mobiles-created
Use static variable to count number of mobiles created


## Run

```powershell
## Run

```powershell
.\apache-maven-3.9.15\bin\mvn.cmd compile exec:java
```

Then open:

```text
http://localhost:9191/
```
```

Then open:

```text
http://localhost:8080/
```
